import Head from 'next/head'
import Loading from './Loading'

const LoadingGroup = () => (
  <>
    <Head>
      <title>Loading Group - TouchGrass</title>
      <meta name='description' content='Loading group information' />
    </Head>

    <Loading />
  </>
)

export default LoadingGroup
