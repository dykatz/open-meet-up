import Head from 'next/head'
import Loading from './Loading'

const LoadingEvent = () => (
  <>
    <Head>
      <title>Loading Event - TouchGrass</title>
      <meta name='description' content='Loading event information' />
    </Head>

    <Loading />
  </>
)

export default LoadingEvent
